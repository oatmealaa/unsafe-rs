use serenity::{
    prelude::*,
    model::prelude::*,
};
pub async fn parse_user(name: &str, guildid: &GuildId) -> Option<UserId> {
    
}

pub async fn random_user(guildid: &GuildId) -> UserId {
    
}
